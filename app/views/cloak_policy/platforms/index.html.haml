%h1 Listing platforms

%table
  %thead
    %tr
      %th Name
      %th Fqdn
      %th Description
      %th Icon
      %th Platform type
      %th They say
      %th We say
      %th Recommendable
      %th
      %th
      %th

  %tbody
    - @platforms.each do |platform|
      %tr
        %td= platform.name
        %td= platform.fqdn
        %td= platform.description
        %td= platform.icon
        %td= platform.platform_type
        %td= platform.they_say
        %td= platform.we_say
        %td= platform.recommendable
        %td= link_to 'Show', platform
        %td= link_to 'Edit', edit_platform_path(platform)
        %td= link_to 'Destroy', platform, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Platform', new_platform_path
